# Multilevel-marketing
A Python-based console application for managing a Multi-Level Marketing (MLM) network with admin and member login. It supports hierarchical sponsor–member relationships and stores data persistently using file handling (pickle).
